mapping_node:
  ros__parameters:
    # Exploration behavior
    exploration_timeout_s: 300.0    # max seconds to explore
    max_scan_drive_cycles: 20       # max head-scan + drive cycles

    # Environment
    use_sim: false
